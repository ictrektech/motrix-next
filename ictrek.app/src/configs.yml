configs:
  - name: MOTRIX_DOWNLOADS_PATH
    type: path
    title:
      zh-CN: 下载目录
      en: Downloads Path
    description:
      zh-CN: 环境变量 MOTRIX_DOWNLOADS_PATH。宿主机上的下载目录，同时保存 aria2 session，默认 /data/vos_workspace/motrix/downloads。
      en: Environment variable MOTRIX_DOWNLOADS_PATH. Host downloads directory, also storing the aria2 session, default /data/vos_workspace/motrix/downloads.
    default: /data/vos_workspace/motrix/downloads
